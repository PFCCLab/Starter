### 姓名

刘宇航

### 开发中的快乐开源任务

【GraphNet 热身打卡】计算图收集

### 本双周工作

1. **任务 X**

参与 GraphNet 社区共建（热身打卡）：

成功完成 PyTorch 框架下 resnet18 模型计算图的捕获与抽取。

编写并提交了自动抽取脚本 extract_resnet18.py，熟练掌握了 graph_net.torch.extractor 的核心 API 调用及输入张量（Tensor）的构造逻辑。

在本地环境独立排查并解决了环境变量（Workspace）配置及 Git PAT 鉴权推送等 Infra 痛点问题。

完成了本地 Validation API 验证，确保样本完全符合 GraphNet 的 8 项数据集约束规则。

已向 PaddlePaddle/GraphNet 官方仓库提交了新增模型样本的 PR。

